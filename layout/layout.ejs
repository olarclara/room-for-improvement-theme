<!DOCTYPE html>
<html lang="<%= config.language %>">
  <%- partial('_partials/head') %>
  <body>
    <div class="content">
      <% if (!is_post() && page.title !== 'talks' && page.title !== 'author') { %>
        <%- partial('_partials/header') %>
      <% } %>
      <%- body %>
    </div>
    <%- partial('_partials/footer') %>
  </body>
</html>